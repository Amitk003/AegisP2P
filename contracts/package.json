{
  "name": "aegisp2p-contracts",
  "version": "1.0.0",
  "description": "AegisP2P smart contracts for ZK-verified fiat-to-crypto escrow on Monad",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:testnet": "npx hardhat run scripts/deploy.js --network monadTestnet",
    "deploy:mainnet": "npx hardhat run scripts/deploy.js --network monadMainnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "@openzeppelin/contracts": "^5.6.1",
    "dotenv": "^17.4.2",
    "hardhat": "^2.28.6"
  }
}
